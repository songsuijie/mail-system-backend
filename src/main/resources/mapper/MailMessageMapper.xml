<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.scut.mailsystem.mapper.MailMessageMapper">

    <resultMap id="MailListItemRowResultMap" type="com.scut.mailsystem.mapper.row.MailListItemRow">
        <id column="mail_id" property="mailId"/>
        <result column="thread_id" property="threadId"/>
        <result column="reply_to_mail_id" property="replyToMailId"/>
        <result column="subject" property="subject"/>
        <result column="content" property="content"/>
        <result column="sender_username" property="senderUsername"/>
        <result column="sender_nickname" property="senderNickname"/>
        <result column="recipient_username" property="recipientUsername"/>
        <result column="recipient_nickname" property="recipientNickname"/>
        <result column="sent_at" property="sentAt"/>
        <result column="read_flag" property="readFlag"/>
        <result column="priority" property="priority"/>
        <result column="spam_flag" property="spamFlag"/>
        <result column="spam_level" property="spamLevel"/>
        <result column="risk_level" property="riskLevel"/>
        <result column="risk_reason" property="riskReason"/>
        <result column="analysis_status" property="analysisStatus"/>
    </resultMap>

    <resultMap id="MailDetailRowResultMap" type="com.scut.mailsystem.mapper.row.MailDetailRow">
        <id column="mail_id" property="mailId"/>
        <result column="thread_id" property="threadId"/>
        <result column="reply_to_mail_id" property="replyToMailId"/>
        <result column="subject" property="subject"/>
        <result column="content" property="content"/>
        <result column="sender_id" property="senderId"/>
        <result column="sender_username" property="senderUsername"/>
        <result column="sender_nickname" property="senderNickname"/>
        <result column="recipient_id" property="recipientId"/>
        <result column="recipient_username" property="recipientUsername"/>
        <result column="recipient_nickname" property="recipientNickname"/>
        <result column="sent_at" property="sentAt"/>
        <result column="sender_deleted" property="senderDeleted"/>
        <result column="read_flag" property="readFlag"/>
        <result column="read_at" property="readAt"/>
        <result column="deleted_flag" property="deletedFlag"/>
        <result column="deleted_at" property="deletedAt"/>
        <result column="spam_flag" property="spamFlag"/>
        <result column="analysis_status" property="analysisStatus"/>
        <result column="summary" property="summary"/>
        <result column="spam_level" property="spamLevel"/>
        <result column="spam_reason" property="spamReason"/>
        <result column="risk_level" property="riskLevel"/>
        <result column="risk_reason" property="riskReason"/>
        <result column="priority" property="priority"/>
        <result column="priority_reason" property="priorityReason"/>
        <result column="reply_suggestions" property="replySuggestions"/>
        <result column="attachment_file_id" property="attachmentFileId"/>
        <result column="attachment_original_filename" property="attachmentOriginalFilename"/>
        <result column="attachment_content_type" property="attachmentContentType"/>
        <result column="attachment_file_size" property="attachmentFileSize"/>
    </resultMap>

    <insert id="insert" parameterType="com.scut.mailsystem.entity.MailMessage"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO mail_message (
            sender_id,
            thread_id,
            reply_to_mail_id,
            subject,
            content,
            attachment_file_id,
            sent_at,
            status,
            sender_deleted,
            created_at,
            updated_at
        ) VALUES (
            #{senderId},
            #{threadId},
            #{replyToMailId},
            #{subject},
            #{content},
            #{attachmentFileId},
            #{sentAt},
            #{status},
            #{senderDeleted},
            #{createdAt},
            #{updatedAt}
        )
    </insert>

    <update id="updateThreadFields">
        UPDATE mail_message
        SET thread_id = #{threadId},
            reply_to_mail_id = #{replyToMailId},
            updated_at = NOW()
        WHERE id = #{mailId}
    </update>

    <select id="countInbox" resultType="long">
        SELECT COUNT(1)
        FROM mail_message m
        INNER JOIN mail_recipient r ON r.mail_id = m.id
        WHERE r.recipient_id = #{userId}
          AND r.deleted_flag = 0
          AND r.spam_flag = 0
    </select>

    <select id="selectInboxPage" resultMap="MailListItemRowResultMap">
        SELECT
            m.id AS mail_id,
            m.thread_id,
            m.reply_to_mail_id,
            m.subject,
            m.content,
            sender.username AS sender_username,
            sender.nickname AS sender_nickname,
            recipient.username AS recipient_username,
            recipient.nickname AS recipient_nickname,
            m.sent_at,
            r.read_flag,
            COALESCE(a.priority, 'MEDIUM') AS priority,
            COALESCE(a.spam_flag, 0) AS spam_flag,
            COALESCE(a.spam_level, 'NONE') AS spam_level,
            COALESCE(a.risk_level, 'SAFE') AS risk_level,
            a.risk_reason,
            COALESCE(a.analysis_status, 'NOT_STARTED') AS analysis_status
        FROM mail_message m
        INNER JOIN mail_recipient r ON r.mail_id = m.id
        INNER JOIN sys_user sender ON sender.id = m.sender_id
        INNER JOIN sys_user recipient ON recipient.id = r.recipient_id
        LEFT JOIN mail_analysis a ON a.mail_id = m.id AND a.recipient_id = r.recipient_id
        WHERE r.recipient_id = #{userId}
          AND r.deleted_flag = 0
          AND r.spam_flag = 0
        ORDER BY m.sent_at DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="countSent" resultType="long">
        SELECT COUNT(1)
        FROM mail_message m
        WHERE m.sender_id = #{userId}
          AND m.sender_deleted = 0
    </select>

    <select id="selectSentPage" resultMap="MailListItemRowResultMap">
        SELECT
            m.id AS mail_id,
            m.thread_id,
            m.reply_to_mail_id,
            m.subject,
            m.content,
            sender.username AS sender_username,
            sender.nickname AS sender_nickname,
            recipient.username AS recipient_username,
            recipient.nickname AS recipient_nickname,
            m.sent_at,
            NULL AS read_flag,
            COALESCE(a.priority, 'MEDIUM') AS priority,
            COALESCE(a.spam_flag, 0) AS spam_flag,
            COALESCE(a.spam_level, 'NONE') AS spam_level,
            COALESCE(a.risk_level, 'SAFE') AS risk_level,
            a.risk_reason,
            COALESCE(a.analysis_status, 'NOT_STARTED') AS analysis_status
        FROM mail_message m
        INNER JOIN mail_recipient r ON r.mail_id = m.id
        INNER JOIN sys_user sender ON sender.id = m.sender_id
        INNER JOIN sys_user recipient ON recipient.id = r.recipient_id
        LEFT JOIN mail_analysis a ON a.mail_id = m.id AND a.recipient_id = r.recipient_id
        WHERE m.sender_id = #{userId}
          AND m.sender_deleted = 0
        ORDER BY m.sent_at DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="selectDetailByMailId" resultMap="MailDetailRowResultMap">
        SELECT
            m.id AS mail_id,
            m.subject,
            m.content,
            m.sender_id,
            sender.username AS sender_username,
            sender.nickname AS sender_nickname,
            r.recipient_id,
            recipient.username AS recipient_username,
            recipient.nickname AS recipient_nickname,
            m.sent_at,
            m.sender_deleted,
            r.read_flag,
            r.read_at,
            r.deleted_flag,
            r.deleted_at,
            COALESCE(a.spam_flag, r.spam_flag, 0) AS spam_flag,
            COALESCE(a.analysis_status, 'NOT_STARTED') AS analysis_status,
            a.summary,
            COALESCE(a.spam_level, r.spam_level, 'NONE') AS spam_level,
            a.spam_reason,
            COALESCE(a.risk_level, r.risk_level, 'SAFE') AS risk_level,
            a.risk_reason,
            COALESCE(a.priority, 'MEDIUM') AS priority,
            a.priority_reason,
            a.reply_suggestions,
            m.attachment_file_id,
            f.original_filename AS attachment_original_filename,
            f.content_type AS attachment_content_type,
            f.file_size AS attachment_file_size
        FROM mail_message m
        INNER JOIN mail_recipient r ON r.mail_id = m.id
        INNER JOIN sys_user sender ON sender.id = m.sender_id
        INNER JOIN sys_user recipient ON recipient.id = r.recipient_id
        LEFT JOIN mail_analysis a ON a.mail_id = m.id AND a.recipient_id = r.recipient_id
        LEFT JOIN file_resource f ON f.file_id = m.attachment_file_id
        WHERE m.id = #{mailId}
        LIMIT 1
    </select>

    <select id="countRelatedMailUser" resultType="long">
        SELECT COUNT(1)
        FROM mail_message m
        LEFT JOIN mail_recipient r ON r.mail_id = m.id AND r.recipient_id = #{userId}
        WHERE m.id = #{mailId}
          AND (m.sender_id = #{userId} OR r.id IS NOT NULL)
    </select>
</mapper>
